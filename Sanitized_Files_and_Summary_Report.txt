
Sanitized Repository Structure:

- src/main/resources/application.yml (AWS configuration blocks removed)
- k8s/eks-deployment.yaml (EKS cluster and service account blocks removed)
- cloudformation/aws-stack.yaml (file deleted)
- k8s/aws-service.yaml (file deleted)
- terraform/aws-infra.tf (file deleted)

---

src/main/resources/application.yml (sanitized):

server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# AWS-specific blocks have been removed for compliance.

---

k8s/eks-deployment.yaml (sanitized):

apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: springboot-app
  template:
    metadata:
      labels:
        app: springboot-app
    spec:
      containers:
        - name: springboot-app
          image: myrepo/springboot-app:latest
          ports:
            - containerPort: 8080

# AWS EKS cluster and service account blocks have been removed.

---

cloudformation/aws-stack.yaml: (file deleted)

k8s/aws-service.yaml: (file deleted)

terraform/aws-infra.tf: (file deleted)

---

Summary Report:

- src/main/resources/application.yml: AWS configuration block removed
- k8s/eks-deployment.yaml: EKS cluster and service account blocks removed
- cloudformation/aws-stack.yaml: file deleted
- k8s/aws-service.yaml: file deleted
- terraform/aws-infra.tf: file deleted
