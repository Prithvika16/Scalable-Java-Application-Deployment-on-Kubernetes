Sanitized Repository Structure:

application.yml:
# (Sanitized content with AWS section removed)

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: user
    password: pass

# Other non-AWS configurations remain

k8s/deployment.yaml:
# (Sanitized content with AWS EKS references removed)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: springboot-app
  template:
    metadata:
      labels:
        app: springboot-app
    spec:
      containers:
      - name: springboot-app
        image: myrepo/springboot-app:latest
        ports:
        - containerPort: 8080

cloudformation/aws-stack.yaml: [REMOVED]

terraform/aws-infra.tf: [REMOVED]

k8s/eks-service.yaml: [REMOVED]

Summary Report:
- application.yml: AWS configuration block removed
- k8s/deployment.yaml: EKS cluster and service account blocks removed
- cloudformation/aws-stack.yaml: file deleted
- terraform/aws-infra.tf: file deleted
- k8s/eks-service.yaml: file deleted
